// Admin landing (the first page after login): an intentionally empty canvas.
// The individual sections live under /admin/*.
export default function AdminIndex() {
  return <div className="h-full" />;
}
